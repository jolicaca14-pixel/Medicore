# Variables de entorno para el backend de MediCore
# NO SUBIR ESTE ARCHIVO A GIT

# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=medicore
DB_PASSWORD=medicore_dev_2024
DB_NAME=medicore_db

# JWT Secrets (CAMBIAR EN PRODUCCIÓN)
JWT_ACCESS_SECRET=medicore_access_secret_key_2024_change_in_production
JWT_REFRESH_SECRET=medicore_refresh_secret_key_2024_change_in_production

# JWT Expiration
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:3000
